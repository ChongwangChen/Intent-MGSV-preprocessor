# Dedicated dependency set for auto.py (Python 3.10).
# Keep this separate from requirements_server.txt so the stable annotation
# service environment is not changed by TensorFlow/PyTorch version resolution.
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
openpyxl==3.1.5
librosa==0.11.0
opencv-python-headless>=4.10,<5
requests>=2.31,<3
ffmpeg-python==0.2.0

# TransNetV2
tensorflow==2.21.0

# BeatNet. BeatNet 1.1.1 omits madmom from its package metadata even though
# it imports it at runtime, so the compatible upstream revision is explicit.
torch==2.12.0
BeatNet==1.1.1
Cython>=3.0,<4
matplotlib>=3.8,<4
mido>=1.3,<2
madmom @ git+https://github.com/CPJKU/madmom.git@27f032e8947204902c675e5e341a3faf5dc86dae
